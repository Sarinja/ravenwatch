{
  "name": "RavenWatch",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "tauri:build": "npm run build && tauri build",
    "check-links": "linkinator ./src/index.html ./README.md --recursive",
    "check-imports": "node ./scripts/check-imports.cjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "glob": "^11.0.1",
    "linkinator": "^3.1.0",
    "vite": "^5.4.21"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-opener": "^2.5.3",
    "tall": "^8.0.0"
  }
}
